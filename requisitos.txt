# Lo que necesita el panel. Se instala con:
#     python -m pip install -r requisitos.txt
flask>=3.0
waitress>=3.0   # servidor de produccion, solo para desplegar en un servidor
openpyxl>=3.1
pdfplumber>=0.11
fpdf2>=2.8      # solo para crear el ejemplo de práctica
